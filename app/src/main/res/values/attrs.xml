<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="TclRefreshHeader">
        <attr name="header_paddingTop" format="dimension" />
        <attr name="header_paddingBottom" format="dimension" />
        <attr name="header_theme" format="enum">
            <enum name="dark" value="0" />
            <enum name="light" value="1" />
        </attr>
    </declare-styleable>

    <declare-styleable name="receipt_common_line">
        <attr name="title" format="string" />
        <attr name="divider_visibility" format="boolean" />
        <attr name="is_must_option" format="boolean" />
        <attr name="title_content_hint" format="string" />
        <attr name="title_content" format="string" />
    </declare-styleable>

    <declare-styleable name="tclTabSelector">
        <attr name="tabSelectedTextColor" format="color" />
        <attr name="tabTextColor" format="color" />
        <attr name="tabSelectedTextSize" format="dimension" />
        <attr name="tabTextSize" format="dimension" />
        <attr name="tabLevel" format="integer" />
    </declare-styleable>

    <!-- 正则表达式编辑框 -->
    <declare-styleable name="RegexEditText">
        <!-- 正则输入限制 -->
        <attr name="inputRegex" format="string" />
        <!-- 常用正则类型 -->
        <attr name="regexType">
            <!-- 手机号（只能以 1 开头） -->
            <enum name="mobile" value="0x01" />
            <!-- 中文（普通的中文字符） -->
            <enum name="chinese" value="0x02" />
            <!-- 英文（大写和小写的英文） -->
            <enum name="english" value="0x03" />
            <!-- 整数（非 0 开头的数字） -->
            <enum name="count" value="0x04" />
            <!-- 用户名（中文、英文、数字） -->
            <enum name="name" value="0x05" />
            <!-- 非空格字符 -->
            <enum name="nonnull" value="0x06" />
        </attr>
    </declare-styleable>

    <declare-styleable name="ucrop_UCropView">

        <!--Crop image view-->
        <attr name="ucrop_aspect_ratio_x" format="float" />
        <attr name="ucrop_aspect_ratio_y" format="float" />

        <!--Overlay view-->
        <attr name="ucrop_show_oval_crop_frame" format="boolean" />
        <attr name="ucrop_oval_dimmed_layer" format="boolean" />
        <attr name="ucrop_dimmed_color" format="color" />

        <attr name="ucrop_grid_stroke_size" format="dimension" />
        <attr name="ucrop_grid_color" format="color" />
        <attr name="ucrop_grid_row_count" format="integer" />
        <attr name="ucrop_grid_column_count" format="integer" />
        <attr name="ucrop_show_grid" format="boolean" />

        <attr name="ucrop_frame_stroke_size" format="dimension" />
        <attr name="ucrop_frame_color" format="color" />
        <attr name="ucrop_show_frame" format="boolean" />
    </declare-styleable>

</resources>